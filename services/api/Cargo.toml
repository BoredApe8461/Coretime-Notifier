[package]
name = "api-service"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0", features=["json"] }
rocket_cors = "0.6.0"

routes = { path = "./routes", package = "api-routes" }
